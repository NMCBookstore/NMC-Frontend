import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  box: {
    borderRadius: 2,
    height: 500,
    display: "block",
    maxWidth: "100%",
    overflow: "hidden",
    width: "100%",
  },
}));
